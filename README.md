# SmartBoard 
## Backend
- FastAPI app: `backend/app/main.py`
- API modules: `backend/app/api/v1/endpoints`
- Layers: `schemas`, `models`, `repositories`, `services`

## Frontend
- React + Vite + TypeScript app: `frontend`
- Pages for auth, feed, ad details, ad create/edit, profile
- Components for feed, recommendations, and filters
